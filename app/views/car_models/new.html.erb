<h1> Cadastrar novo Modelo de Carro </h1>

<p> Formulario 1</p>
<%= form_with(model: @car_model, local:true) do |f| %> 
    <%= f.label :name, 'Nome:'%>
    <%= f.text_field :name %>

    <%= f.label :motorization, 'Motorization:'%>
    <%= f.text_field :motorization %>

    <%= f.label :year, 'Ano:'%>
    <%= f.text_field :year %>

    <%= f.label :fuel_type, 'Combustivel:'%>
    <%= f.text_field :fuel_type %>

    <%= f.label :manufacturer, 'Fabricante:'%>
    <%= f.collection_select(:manufacturer_id, @manufacturers, :id, :name) %>
        <!-- selecionar um objeto de uma selecao, quero que pegue o id, mas mostre o name -->
        <!-- o rails roda um map de acordo com o @manufacturers, se ele não achar é pq o @manufacturers está vazio -->

    <%= f.label :car_category_id, 'Categoria:'%>
    <%= f.collection_select(:car_category_id, @categories, :id, :name) %>
    
    <%= f.submit 'Enviar' %>
<% end %>

